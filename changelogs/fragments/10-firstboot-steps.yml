---
minor_changes:
  - Introduced the ``steps`` role, a declarative, ordered step framework
    whose steps can consume each other's outputs.
  - Reworked the ``firstboot`` role to use the new step framework
    instead of hard-coded task files.
